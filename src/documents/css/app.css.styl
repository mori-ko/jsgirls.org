@import 'reset.css'

emphasis-yellow = #f0db4f

body {
  color: #000;
  background: #fff;
  font-size: 20px;
  font-weight: normal;
  font-family: "Helvetica Neue", "Helvetica", Helvetica, Arial, sans-serif;
}